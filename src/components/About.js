import React from 'react';
import { about } from './about-data/about';

const About = () => {
    return (
        <div className='about'>
            <h1>{about}</h1>
        </div>
    );
};

export default About;
